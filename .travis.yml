language: python
python:
  - 3.4
  - 3.5
  - 3.6
install:
  - python setup.py install
  - pip install coverage
  - pip install codecov
  - pip install pytest
script: coverage run setup.py test
after_success:
  - codecov
